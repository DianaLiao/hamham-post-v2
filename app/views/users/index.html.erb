<h1>All the Hams</h1>
<table>
  <tr><th>Name</th><th></th></tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= link_to user.full_name, user_path(user) %></td>
    <td><%= button_to "Chat", {:controller => "boards", :action => "user_convo", board:{:user_id => session[:user_id], :other_user_id => user.id}}, method: :post %></td>
  </tr>
  <% end %>
</table>