<!DOCTYPE html>
<html>
  <head>
    <title>HamhamPostV2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">  



    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  </head>
  


  <body>
    <header>
      <h1 class = "site-title">Ham-Ham Post</h1>
      <% if user_signed_in? %>
      <marquee behavior="scroll" direction="right" scrollamount="10">
        <span><%= session[:marquee]%></span><%=image_tag("stanley.jpg")%>
      </marquee>
      <% end %>
    </header>
    <nav class="top-nav">
      <ul>
        <li><%= link_to "Welcome", root_path %></a></li>
        <% if user_signed_in? %>
              <li><%= link_to "Boards", boards_path %></li>
              <li><%= link_to "Users", users_path %></li>
              <li><a href="../users/my_boards">My Boards</a></li>
              <li><%= link_to "Profile", user_path(current_user) %></li>
              <li><%= link_to("Sign Out", destroy_user_session_path, :method => 'delete') %></li>
            <% else %>
              <li><%= link_to("Sign In", new_user_session_path) %></li>
              <li><%= link_to("Register", new_user_registration_path) %></li>
            <% end %></li>
      </ul>
    </nav>
    <!-- <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p> -->
    <div class="flash-hash">
      <%= content_tag(:div, flash[:error], :id => "flash_error") if flash[:error] %>
      <%= content_tag(:div, flash[:notice], :id => "flash_notice") if flash[:notice] %>
      <%= content_tag(:div, flash[:alert], :id => "flash_alert") if flash[:alert] %>
    </div>
    <div class = "sub-body">
    <%= yield %>
    </div>
  </body>
</html>
